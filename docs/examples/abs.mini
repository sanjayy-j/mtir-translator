// The worked example from Figure 2 of the Review 1 report.
// Traced by hand through CIR, LLVM IR, WebAssembly and stack bytecode.

fn abs(x: int) -> int {
    if (x < 0) { return -x; }
    return x;
}

fn main() -> int {
    print_int(abs(-7));
    return 0;
}
