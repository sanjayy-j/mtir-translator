"""CIR reference interpreter -- the differential oracle.

Module M8a.  Owner: Member 2.
Status: PLANNED -- Week 8.
"""
def run(module, entry="main", args=()):
    """Execute a CIR module directly under the semantics fixed in
    docs/divergence.md, and return (return_value, stdout, trapped).

    This is the oracle for the differential harness: every back end is
    compared against it, not against one another.
    """
    raise NotImplementedError(
        "CIR interpreter is scheduled for Week 8 (M8a, Member 2)")
